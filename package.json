{
  "name": "multichess",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npx tsc",
    "start": "node backend/dist/index.js"
  }
}
